---
"@kixvault/shared": patch
"@kixvault/api": patch
---

Added `POST /api/sneakers/from-catalog` to re-fetch catalog product data server-side on create and moved manual create to `POST /api/sneakers/custom`.
